<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cheny.mapper.EmpMapper">

    <resultMap id="BaseResultMap" type="com.cheny.domain.entity.Emp">
            <id property="id" column="id" />
            <result property="username" column="username" />
            <result property="password" column="password" />
            <result property="name" column="name" />
            <result property="gender" column="gender" />
            <result property="phone" column="phone" />
            <result property="job" column="job" />
            <result property="salary" column="salary" />
            <result property="image" column="image" />
            <result property="entryDate" column="entry_date" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="Base_Column_List">
        id,username,password,name,gender,phone,
        job,salary,image,entry_date,create_time,
        update_time
    </sql>
    <!-- 新增：员工左连接部门，返回EmpVO -->
    <select id="selectEmpVoPage" resultType="com.cheny.domain.vo.EmpVo">
        SELECT
        e.id,
        e.name,
        e.gender,
        e.job,
        e.image,
        d.name AS deptName,
        e.entry_date,
        e.update_time
        FROM emp e
        LEFT JOIN dept d ON e.dept_id = d.id
        ${ew.customSqlSegment}
    </select>
    <select id="countByEmp" resultType="java.util.Map">
        SELECT
        job AS job,
        COUNT(*) AS cnt
        FROM emp
        GROUP BY job
        ORDER BY cnt DESC
    </select>
</mapper>
